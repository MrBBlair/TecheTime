# API Environment Variables
PORT=3001

# Firebase Configuration
# ======================
# 
# Authentication Method (choose one):
#
# Option 1 (Recommended): Application Default Credentials (ADC)
#   - No JSON keys needed - works even if key creation is blocked by org policies
#   - Run: gcloud auth application-default login
#   - See: apps/api/src/scripts/setup-adc.md for detailed instructions
#   - Just set FIREBASE_PROJECT_ID below, leave FIREBASE_SERVICE_ACCOUNT unset
#
# Option 2: Service Account JSON Key
#   - Download from Firebase Console → Project Settings → Service Accounts
#   - See: apps/api/src/scripts/get-service-account.md for instructions
#   - Set FIREBASE_SERVICE_ACCOUNT with the JSON content (single line)
#
# Option 3: Firebase Emulators (for local testing only)
#   - Set USE_FIREBASE_EMULATOR=true
#   - Still requires authentication (ADC or service account)
#   - See: apps/api/src/scripts/setup-adc.md for ADC setup

# Required: Your Firebase Project ID
FIREBASE_PROJECT_ID=your-project-id

# Optional: Service Account JSON (only if not using ADC)
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"...","private_key":"...","client_email":"..."}

# Set to 'true' to use Firebase emulators (requires emulators to be running)
USE_FIREBASE_EMULATOR=false

# Optional: Firebase Web API Key (required for Kiosk exit verification)
# Same as VITE_FIREBASE_API_KEY in apps/web. Used to verify admin password when leaving Kiosk mode.
# FIREBASE_WEB_API_KEY=your-firebase-web-api-key

# Optional Services
POSTMARK_API_TOKEN=
POSTMARK_FROM_EMAIL=
GOOGLE_AI_API_KEY=
FRONTEND_URL=http://localhost:5173
