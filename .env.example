# Tech eTime – Environment Variables
# Copy this file to .env (root) and apps/api/.env, then fill in values.
# Do not commit .env or real keys to version control.

# ============== Web (apps/web) ==============
# Create .env in apps/web or set in Vercel for production
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
# Optional: API URL when frontend is deployed separately (e.g. https://api.yourdomain.com)
VITE_API_URL=

# ============== API (apps/api) ==============
PORT=3001
FIREBASE_PROJECT_ID=
# Firebase Service Account JSON (single line or use Vercel env)
FIREBASE_SERVICE_ACCOUNT=
POSTMARK_API_TOKEN=
POSTMARK_FROM_EMAIL=
GOOGLE_AI_API_KEY=
FRONTEND_URL=
